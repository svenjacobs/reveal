<?xml version="1.0" encoding="utf-8"?>
<!--
    This module hosts instrumentation and screenshot tests only. The tests compose their own
    content via createComposeRule(), which runs in the ComponentActivity provided by
    androidx.compose.ui.test.manifest, so no application class, activity or theme is needed here.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:supportsRtl="true" />

</manifest>
